// there are two main representation of a graph
// dijkstra returns the shortest path to the destination (sink) from the starting node (source)
export default function dijkstra_list(source: number, sink: number, arr: WeightedAdjacencyList): number[] {

}