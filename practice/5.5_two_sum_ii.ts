// instead of an unsorted array, we are now given a sorted one
// but we are now limited to using O(1) amount of memory
// instead of using one pointer to look items in an array, how about having two instead?
// find a O(n) or better time complexity solution
export default function two_sum(arr: number[], target: number): number[] {

}