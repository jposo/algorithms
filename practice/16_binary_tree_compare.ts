// how can be compare if two binary trees are equal?
// how can we maintain a tree structure to confirm?

// hint: use recursion
export default function compare(a: BinaryNode<number> | null, b: BinaryNode<number> | null): boolean {

}