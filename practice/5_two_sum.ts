// in this problem, we are given an UNSORTED array of numbers
// our goal is to find the two indexes in the array that their values sum to the target
// is there maybe a way to keep track of the number we are looking for? (HINT:              use an object/map, create map in TS with const map = new Map<number, number>())
export default function two_sum(arr: number[], target: number): number[] {

}