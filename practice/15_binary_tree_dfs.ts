// dfs maintains the structure of the tree while traversing it
// this uses a stack instead of a queue like bfs
// so instead of creating an array to act like a stack
// use recursive calls instead
// create a Depth-first-search function (recursive) to find a value in a binary tree
export default function dfs(head: BinaryNode<number>, needle: number): boolean {

}
