// find the BEST way to find the floor (index) in which the crystal ball breaks (better than O(N))
// you only have two crystal balls, so you cant break more than two of them
// (floors' array is false until the crystal ball breaks)
export default function two_crystal_balls(floors: boolean[]): number {

}